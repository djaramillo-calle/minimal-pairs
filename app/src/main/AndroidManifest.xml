<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Clip-pack download from GitHub Releases and rendering the pack with the learner's own
         Azure key (docs/DESIGN.md). The Say-it mode uses no network at all: it plays a clip the
         coach rendered, records, writes the file, and the cloud scores it — the key never
         reaches the phone for it (docs/CONTRACT.md, "sayit.zip"). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Say it records the learner reading a whole sentence (docs/CONTRACT.md). Asked for at
         runtime the first time he taps record, with a rationale; refused, the mode degrades to
         listening and reading along rather than disappearing, and never asks again by itself. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- RECORD_AUDIO would otherwise imply a required microphone and keep the app off devices
         that have none; Say it degrades gracefully there and the drill is unaffected. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinimalPairs">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
